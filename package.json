{
  "name": "linkhub",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute linkhub-db --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute linkhub-db --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.110.0",
    "typescript": "^5.6.0",
    "@cloudflare/workers-types": "^4.20260702.1"
  }
}
